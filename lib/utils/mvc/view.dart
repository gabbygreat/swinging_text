part of 'controller.dart';

class MvcView extends StatelessView<MvcScreen, MvcController> {
  const MvcView(super.state, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
